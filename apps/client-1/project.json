{
  "name": "client-1",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/client-1/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/client-1"
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "client-1:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "client-1:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "client-1:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "options": {
        "buildTarget": "client-1:build"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
